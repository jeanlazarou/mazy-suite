// Shared layout constants (mirrored by --head-w in styles.css).
export const HEAD_WIDTH = 150;
export const LANE_PAD = 8; // envelope vertical padding inside a lane (px)
export const TRACK_HEIGHT = 110;
export const MASTER_HEIGHT = 64;
