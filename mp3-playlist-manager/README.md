# MP3 playlist manager

## Usage

```bash
# Using poetry run
poetry run playlist-manager -d input_dir -o output_dir -l playlist.json list

# Or activate the virtual environment and run directly
poetry shell
playlist-manager -d input_dir -o output_dir -l playlist.json cover -c cover.jpg
```