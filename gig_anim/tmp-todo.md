when ?

to replace "playlist" with "tracks"?

To use this app, you would need to:

Set up a new React project using Create React App or a similar tool.
Install the required dependencies, particularly framer-motion for animations.
Place your album JSON files, cover images, and SRT files in the appropriate directories within the public folder of your React app.
Implement a method to change tracks during the performance (this could be done through user input or automation).

To further enhance this application, you might consider:

Adding a audio playback feature to synchronize the lyrics with the music.
Implementing a control panel for the performers to manage the display.
Creating visual effects that react to the music's rhythm or volume.
Adding support for multiple languages or translations.
Incorporating audience interaction features, like displaying tweets or messages from the audience.

keep random values history to relay

websocket?

- teleprompter send start(song), stop(song) to server
- gig anim receives start and stop requests
- other type of animation, example display a grid with all lyrics, repeat if no enough, and make them got from "off-state" to a very light color, and restart

editor mode

https://ui.shadcn.com/docs/components/popover